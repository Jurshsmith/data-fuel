
[package]
name = "data-fuel-benches"
edition = "2021"
version = "0.0.1"

[dependencies]
linked-lists = { path = "../linked-lists" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "benchmarks"
harness = false