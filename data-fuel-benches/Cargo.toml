
[package]
name = "data-fuel-benches"
edition = "2021"

[dependencies]
linked-lists = { path = "../linked-lists" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "benchmarks"
harness = false